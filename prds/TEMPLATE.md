# PRD-20260722-export-report（示例标题，一级标题可用中文）

> **文件名即需求 id**,必须满足协议正则 `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`,并通过 protocol.md §3 的跨平台限制(禁中文、空格、`..`、路径分隔符、`.lock` 后缀及 Windows 保留名)。
> 合法文件名示例:`PRD-20260722-export-report.md`。**Markdown 一级标题可以写中文**(如"# 导出报表优化"),但文件名里的 id 段不能带中文。
> 放入 prds/ 后对 AI 代理说"读 AGENTS.md,执行需求 <id>"即可启动流水线(id = 去掉 .md 的文件名)。

## 背景
为什么做:业务问题或目标,一段话。

## 功能描述
做什么:功能点逐条列出,涉及界面的附交互说明或原型链接。

## 验收标准(必填,AI 以此判定"完成")
逐条、可核对。好的写法:"在 XX 页面按 YY 筛选后,导出文件包含 ZZ 字段且与页面数值一致"。
1.
2.

## 范围外(明确不做的)
-

## 补充信息(可选)
- 涉及系统的猜测(不确定可不填,AI 会按 config/projects.md 的判定信号判定)
- 期望上线时间 / 相关干系人 / 参考资料
