# 人工闸口与风险策略

- 闸口 A：存在会导致不同实现的阻塞性歧义时触发。
- 闸口 B：R2/R3 必须触发；R0/R1 仅当项目配置明确声明自动策略时才能跳过。
- 闸口 C：合并或无 MR 验收时始终需要明确确认。

批准必须记录日期、原话和适用范围。沉默或含糊回复不构成批准。

## 风险分级

- R0：不改变运行行为。
- R1：局部、可逆、有既有模式和验证覆盖。
- R2：公共 API、数据结构、权限、安全、多项目或明显架构变化。
- R3：生产操作、不可逆变化或无法安全回滚。

生产操作不因批准而自动获得执行权限；批准只允许工作流继续生成安全的代码或交付物。
